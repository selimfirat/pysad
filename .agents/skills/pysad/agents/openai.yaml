interface:
  display_name: "Pysad"
  short_description: "Repo-specific patterns and workflows for pysad"
  default_prompt: "Use the pysad repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true